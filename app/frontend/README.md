# Frontend

React + Vite + Tailwind + shadcn/ui dashboard.

The current operator command center lives in `/src` at the repo root. This folder is reserved for future split (e.g., separate sales site or client portal).
